import Ipod from './Components/Ipod';
function App() {
  //All the components have been derived from the component folder to maintain the structure.
  //This has been done to initialise the state and dependancies which are too important.
  //This is small assignment but this component tends to get filled up which too much stuff.
  return (
    <div className="App">
      <Ipod />
    </div>
  );
}

export default App;